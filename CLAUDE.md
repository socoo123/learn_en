# CLAUDE.md — 英语学习笔记本

本项目是一个 React + Vite 的中英对照英语学习网站，用户（学习者）长期用它积累真实材料来提升英语。
**每次会话开始时，先读本文件了解学习者画像与工作流。**

---

## 一、学习者画像（重要背景）

- **背景**：外企工作约 6 年。
- **现状**：能听懂基本的英语会议，但多数时候需要开英文字幕；口语较弱；**词汇量偏弱**，整体水平长期"不上不下"。
- **目标**：把"能看懂"逐步提升到"能用出来"——重点补词汇与语感。
- **界面偏好**：主要用**浅色主题**，喜欢**高对比、科技感**的样式（电光蓝主色、等宽字体标签）。

> 这意味着：选词讲解时要**偏向真实工作场景**（会议、邮件、跨文化沟通、短语动词、地道表达），而不是考试词汇。

---

## 二、添加新课程的标准流程

用户会以下列方式之一提供材料：
1. 直接贴一段英文
2. 给一个 `.txt` 文件路径
3. 给一个 PDF 路径（扫描图片型 PDF 效果差，需提前说明）

收到材料后，**分析并追加一个对象到 `src/data/lessons.js` 的 `LESSONS` 数组**（排序由前端处理，追加即可）。每篇字段：

| 字段 | 说明 |
|------|------|
| `id` / `date` | 唯一标识 / `YYYY-MM-DD`（同一天多篇用 `日期-2`）。不指定就用今天 |
| `title` | 中文摘要标题（页面会显示成 `日期　标题`） |
| `source` / `tags` | 来源（会议/TED/邮件/文章…）/ 标签 |
| `passage` | 中英对照逐句 `[{ en, zh }]` |
| `core` | 右侧标注栏：核心词 5~8 个 + 核心语法 1~3 条 |
| `vocab` | 底部完整生词表（音标/词性/释义/例句） |
| `grammar` | 底部语法详解 `{ pattern, rule, examples }` |
| `sentences` | 难句分析（可选） |
| `quiz` | 3~5 道练习：`choice` / `fill`（用 `____` 标空）/ 阅读理解，每题带 `answer` + `explain` |

写完后**用一两句话告诉用户新增了哪一课**。Vite HMR 会自动刷新页面，无需重启。

---

## 三、关键规则 A：用户补充生词（真实盲区）

用户阅读时会遇到"**不认识、但我没收录进 vocab**"的词，会把该词（或含该词的句子）直接贴给我。

处理方式：
1. 把这个词补进**对应那一课的 `vocab`**（必要时也加进 `core`）——给出音标、词性、中文释义、例句。
2. 用户没指明是哪一课时 → 默认补进**最近一课**。
3. **不要为此新建一课**，除非用户明确要开新主题。
4. 补完后简短确认，例如"已把 `leverage` 加进 7/26 那课的生词表"。

> 这些"事后补"的词是评估用户真实水平**最准的信号**，比我能猜到的都准。务必认真收录。

---

## 四、关键规则 B：动态评估真实水平

- 持续积累用户补充过的生词（记录在案），观察其**难度与类别**：日常高频？商务术语？短语动词？习语？正式/口语？
- 当积累到一定量，或**用户主动问起水平**时，给出简短评估：当前强项 / 弱项 / 下一步建议重点。
- 随材料积累，**逐步调整选材与讲解难度**：太简单的少讲，反复出错的重点讲。
- 评估要诚实、具体、有依据（引用 ta 补过的词），不要空泛鼓励。

---

## 五、技术备忘（给 Claude 自己）

- 纯前端，无后端。所有数据在 `src/data/lessons.js`。
- 启动：`npm run dev`（http://127.0.0.1:5900 ，端口固定，浏览器自动开）。构建：`npm run build`。
- 状态持久化两个 key：
  - `en_reader_v2`：UI 偏好（当前课程、主题、中文展开、当前视图 `home`/`lesson`）
  - `en_reader_progress`：学习进度 `{ version:1, updatedAt, lessons: { [id]: { done, updatedAt } } }`，可导出/导入 `en-progress.json`
- 两视图：首页堆叠月块（`Home.jsx`）↔ 阅读三栏（LessonView + Annotations）。未完成月默认展开，整月勾完默认收起；手动展开/收起会话内优先。
- 组件职责：
  - `Home.jsx` — 首页：年分组 + 月块堆叠 + 日条目勾选 + 搜索 + 进度导出/导入
  - `ProgressBits.jsx` — DoneCheckbox / MiniBar
  - `useProgress.jsx` + `lib/progress-store.js` — 进度 Context 与 localStorage
  - `LessonView.jsx` — 中间阅读区 + 工具栏（回首页 / 完成本课 / 中文 / 主题 / PDF / 上下课）
  - `Passage.jsx` — 中英对照 + 核心词高亮联动
  - `Annotations.jsx` — 右侧核心词/语法（与正文双向高亮）
  - `VocabTable.jsx` — 紧凑生词表（例句可展开）
  - `Grammar.jsx` — 语法详解 + 难句分析
  - `Quiz.jsx` — 练习题（选择/填空/阅读理解，一键显示答案）
- PDF 导出 = html2canvas + jsPDF（失败回退 `window.print()`）；导出时自动展开中文与答案。
- 改样式去 `src/styles.css`（CSS 变量在 `:root` 和 `[data-theme="dark"]`）。
- 交互细节见根目录 `PLAN.md`。
